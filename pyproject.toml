[project]
name = "justopinion"
version = "0.4.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anchorpoint>=0.8.2",
    "eyecite>=2.7.6",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "python-ranges>=1.2.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest-recording>=0.13.4",
    "pytest>=9.0.3",
]
